# 4 hours timeout for Windows build (default is 2 hours)
task_timeout: 34400

build_parameters:
  - "--suppress-variables"
  - "--error-overlinking"

channels:
  - https://staging.continuum.io/preprod-pbp/fs/llvmdev-feedstock/pr46/4dbecd1

aggregate_check: false
