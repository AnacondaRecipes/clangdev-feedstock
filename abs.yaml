aggregate_check: false

build_parameters:
  - "--suppress-variables"
  - "--error-overlinking"

# Build against LLVM 21 Stage 0 output
channels:
  - https://staging.continuum.io/prefect/llvm-21.1.8-stage0-build-0
